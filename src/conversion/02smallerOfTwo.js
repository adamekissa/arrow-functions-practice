/**
 * @param {number} a first number
 * @param {number} b second number
 * @returns {number}  */
let smallerOfTwo = (a, b) => {
    if (a < b) {
        return a;
    } else {
        return b;
    }
}
export { smallerOfTwo };